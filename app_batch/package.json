{
  "name": "app_batch",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "rimraf dist && tsc && tsc-alias",
    "dev": "cross-env BATCH_SCHEDULE=development tsx src/index.ts",
    "start": "cross-env BATCH_SCHEDULE=production tsx src/index.ts",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "app_common": "*",
    "cheerio": "^1.1.2",
    "domhandler": "^5.0.3",
    "ioredis": "^5.7.0",
    "node-cron": "^4.2.1"
  },
  "devDependencies": {
    "@types/node-cron": "^3.0.11",
    "tsc-alias": "^1.8.16"
  }
}